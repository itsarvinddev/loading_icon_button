name: loading_icon_button
description: Loading button with icon and text in Flutter. It's working on all flutter platforms. It's a simple way to show a loading button.
version: 1.0.0
homepage: https://github.com/mohesu/loading_icon_button.git

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
